import { Section } from "@/components/layout";
import { StudyHeading } from "@/components/ui/StudyHeading";

export function SettingsPage() {
    return (
        <Section>
            <StudyHeading size="lg">Settings</StudyHeading>
        </Section>
    );
}